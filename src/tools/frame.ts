/*todo remove file
import * as BABYLON from 'babylonjs';
import { IFrame } from './../model/IAppState';
import { babylonToCleanVector } from './vectors';

export function babylonMeshToFrame(mesh: BABYLON.AbstractMesh): IFrame {
    return {
        position: babylonToCleanVector(mesh.position),
        rotation: mesh.rotationQuaternion!.toEulerAngles(),
    };
}
*/
